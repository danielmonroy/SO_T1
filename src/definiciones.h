/* 
López Monroy Luis Daniel
Cuenta: 311309649
*/

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define PI 3.1416
#define formulaTriangulo(base, altura) ((base * altura) / 2)
#define formulaCirculo(radio) (PI * radio * radio)
#define formulaCuadrado(lado) (lado * lado)
#define formulaRectangulo(base, altura) (base * altura)

float circulo(float);
float triangulo(float,float);
float cuadrado(float);
float rectangulo(float,float);